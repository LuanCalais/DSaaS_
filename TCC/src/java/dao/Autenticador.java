package dao;

import modelo.Usuario;

public class Autenticador {

    /*
    public static Usuario AutenticaEmail(Usuario usuario){
        
        UsuarioDAO dao = new UsuarioDAO();
        Usuario verifica = dao.VerificaEmail(usuario);
        
        //se o e-mail estiver diferente de null no bd, dispara um erro
        if(verifica.getEmail() != null){
            verifica = null;
            return verifica;
        }else{
            return usuario;
        }
    }*/
    
}
